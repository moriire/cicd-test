# workflow
name: start sample
on:
  push:
    branches: ["master"]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        name: start
      - run: echo hello this is my first action
        shell: bash
